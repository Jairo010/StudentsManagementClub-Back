const { describe } = require('@jest/globals');
const { existUser, createUser, login, getResetToken, resetPassword, authMiddleware } = require('./modelo');
const supertest = require('supertest');

describe('logIn', () =>{})